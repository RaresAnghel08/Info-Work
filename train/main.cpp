#include <bits/stdc++.h>

using namespace std;
// vector <int> v(n);
int main()
{
    int n;
    cin>>n;
    vector <int> v(n);
    for(int i=0;i<n;i++)
    {
        int x;
        cin>>x;
        // v.push_back(x);
        v[i]=x;z
    }
    for(int i=0;i<n;i++)
            cout<<v[i]<<' ';
    return 0;
}